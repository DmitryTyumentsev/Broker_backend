package algo

func firstChar(s string) (rune, int, bool) {
	runes := []rune(s)
	for k, v := range runes {
		v
	} //сделать не смог. по логике - мне нужно из слайса рун перебрать каждый элемент и понять где значение встречается один раз
	//как написать это в коде - просидел часа полтора с перерывом, посмотрел твою подсказку и 07п - не написал ничего

}
